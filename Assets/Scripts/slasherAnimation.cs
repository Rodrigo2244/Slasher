using UnityEngine;
using System.Collections;

public class slasherAnimation : MonoBehaviour {
	
	// Use this for initialization
	void Start () {
		animation.wrapMode = WrapMode.Loop;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
